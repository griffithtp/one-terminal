# <%= widgetTitle %>

Run the dev server:

```sh
npm run dev --workspace apps/<%= widgetName %>
# → http://localhost:<%= widgetPort %>
```

The widget is registered with the launcher automatically — restart the
Terminal to see it.
