{
  "name": "@<%= orgScope %>/tauri-webview-host",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal Tauri host that opens a single external webview. Spawned by the CDA engine router when an app requests a pinned WebView2 fixed-runtime.",
  "scripts": {
    "tauri": "tauri",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
